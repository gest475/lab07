name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtest-dev libgmock-dev lcov

      - name: Configure CMake with coverage
        run: cmake -H. -B_build -DBUILD_TESTS=ON -DENABLE_COVERAGE=ON

      - name: Build
        run: cmake --build _build

      - name: Run tests
        run: ./_build/check

      - name: Generate coverage report
        run: |
          cd _build
          # Игнорируем ошибки mismatch и unexecuted block
          lcov --directory . --capture --output-file coverage.info \
               --ignore-errors empty,mismatch,unexecuted \
               --rc geninfo_unexecuted_blocks=1
          # Убираем системные файлы
          lcov --remove coverage.info '/usr/*' '*/tests/*' '*/third-party/*' \
               --output-file coverage.info --ignore-errors unused
          # Выводим статистику
          lcov --list coverage.info

      - name: Upload to Coveralls
        uses: coverallsapp/github-action@v2
        with:
          file: _build/coverage.info
          github-token: ${{ secrets.GITHUB_TOKEN }}
          format: lcov
